import authReducer, { loginUser } from "../slices/authSlice";

const initialState = {
  status: null,
}



describe('authSlice', () => {
  it.todo('should change status with "auth.pending" action');
  it.todo('should change status with "auth.fulfilled" action');
  it.todo('should change status with "auth.rejected" action');

})